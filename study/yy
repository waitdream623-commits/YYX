iota()
patial_sum()
模板类
greater<>

# 大二学生的算法训练与大学生活指南

## 一、当前判断

我目前处于算法竞赛的入门阶段。

已有优势：

- 对算法本身有兴趣，享受通过奇妙想法解决问题；
- 课程成绩较好，学习和应试效率不差；
- 已学习基础算法、DP、线段树等内容；
- 愿意复盘和思考，不满足于只记模板；
- 正在争取进入学校 ACM 实验室。

目前的主要问题：

- 暑假中断后，旧知识遗忘，模板调用不稳定；
- CF 约 1000 分，说明基础题识别和实现能力还需要加强；
- 混合比赛中会做的题较少，遇到陌生题时建模经验不足；
- 有时依赖 AI 找规律，容易把“看懂解法”误认为“自己会做”；
- 容易受到权威、排名和他人观点影响，快速怀疑自己的判断；
- 可能把一次比赛成绩与个人价值联系起来。

当前结论：

> 我可以认真冲击 ACM 招新，但暂时没有足够证据把整个大学阶段都押在 ACM 上。

招新是一次值得争取的机会，但不是对能力和人生价值的最终判决。

---

## 二、算法学习的总原则

### 1. 训练目标不是学完更多算法，而是提高独立解决问题的能力

一个知识点真正掌握，至少要经过四个层次：

1. 能说清它解决什么问题；
2. 能解释它为什么正确；
3. 能在没有模板的情况下写出核心代码；
4. 能在没有题目标签时识别它是否适用。

只看课程、背模板、看题解，最多完成前一部分。

### 2. 基础能力优先于高级知识数量

现阶段的优先级：

1. 模拟、枚举、排序、前缀和、差分；
2. 二分、双指针、贪心；
3. BFS、DFS、并查集；
4. 基础 DP；
5. 常用数据结构；
6. 线段树、树状数组等中级结构；
7. 更高级的 DP、图论和博弈。

如果基础题不能稳定完成，继续学习更复杂的算法，收益会下降。

### 3. 题目难度必须与当前能力匹配

训练不能只做让自己完全不会的题。

合理的题目结构可以是：

- 60%：独立完成或稍加提示能完成；
- 30%：认真思考后看提示能学会；
- 10%：明显超出当前水平，用来开阔视野。

如果大多数题都只能看题解，说明训练难度过高。

---

## 三、招新前的训练方案

### 第一步：做一次完整模拟

找一套学校往届招新题、模拟题或难度接近的比赛。

要求：

- 按正式时间完成；
- 不看题解；
- 不让 AI 直接给思路；
- 记录每道题卡住的时间和原因。

每道题归入以下类别：

- 题意没有读懂；
- 读懂了但没有思路；
- 有思路但不会证明；
- 思路正确但代码写不出；
- 会做但模板或细节忘记；
- 本来会做，但比赛中没有识别出来。

不要只记录“这题不会”。

### 第二步：按照失分原因复习

不同问题采用不同办法：

| 问题 | 处理方式 |
|---|---|
| 概念忘记 | 回看课程，随后闭卷复述 |
| 模板忘记 | 不看资料重写核心代码 |
| 不会识别 | 做不标注算法的混合题 |
| 思路不完整 | 学习状态、转移、不变量和证明 |
| 代码错误 | 做边界测试、调试和对拍 |
| 比赛策略差 | 训练先易后难、及时跳题 |

### 第三步：每天的基本结构

如果每天有两小时：

- 15 分钟：复习旧知识，闭卷回忆；
- 60—75 分钟：学习一个小知识点或完成两三道题；
- 30 分钟：调试、补题、整理错误；
- 10 分钟：记录当天真正学会了什么。

如果有三小时，增加整场模拟或一道较难题，不要简单延长刷题时间。

### 第四步：招新前不要无限扩展新专题

如果距离考试很近，重点是：

- 恢复基础模板；
- 提高简单题稳定性；
- 训练读题与选题；
- 保证代码正确；
- 做往届题和模拟题。

不要为了“显得准备充分”而连续学习大量新算法。

---

## 四、知识遗忘的复习系统

### 1. 建立知识清单

每个知识点只保留一页笔记：

```text
名称：
解决什么问题：
适用条件：
核心思路：
复杂度：
常见错误：
代表题：
我是否能闭卷实现：
下次复习时间：
```

知识点状态分为：

- A：能解释、能实现、能做题；
- B：思路理解，但实现不熟；
- C：概念或适用条件模糊。

优先复习 C，再复习 B。不要因为 A 类内容看起来熟悉就反复重看。

### 2. 使用间隔复习

一个知识点可以这样安排：

- 当天：学习并完成基础题；
- 两天后：闭卷回忆和重写关键代码；
- 一周后：做一道变式题；
- 三周后：在混合训练中再次调用。

如果忘记了，不要立刻从头看完整课程。先确定忘的是：

- 定义；
- 适用条件；
- 推导；
- 代码；
- 题目识别；
- 边界处理。

只补真正遗忘的部分。

### 3. DP 的复习重点

不要只背“这是背包”或“这是区间 DP”。

每道 DP 题都尝试回答：

1. 状态表示什么？
2. 状态为什么足够描述过去的信息？
3. 转移从哪里来？
4. 初始状态是什么？
5. 遍历顺序为什么正确？
6. 如何证明不会漏解或重复计算？
7. 复杂度能否通过？

如果不能解释状态定义，说明还没有真正掌握。

### 4. 线段树的复习重点

至少闭卷完成：

- 单点修改、区间查询；
- 区间最大值或区间和；
- 边界情况测试；
- 解释节点维护的信息；
- 解释左右子节点如何合并；
- 说明为什么复杂度是 `O(log n)`。

如果学过懒标记，再解释：

- 标记表示什么；
- 为什么需要下传；
- 标记如何合并；
- 区间修改为什么影响节点信息。

不要只背代码。要知道什么时候根本不该使用线段树。

### 5. SG 函数的复习方式

不要只记结论。

至少掌握：

```text
SG(x) = mex({SG(y) | x 可以转移到 y})
```

并理解：

- 终止状态的 SG 值为什么是 0；
- SG 为 0 通常意味着必败状态；
- 多个独立游戏为什么可以异或；
- 如何用小规模暴力程序验证 SG 结果。

做过一道依赖 AI 找规律的题后，第二天必须关掉题解重新做。否则只能算“看过”，不能算“掌握”。

---

## 五、比赛和 AI 的使用规则

### 比赛中

遵守以下流程：

1. 先浏览所有题；
2. 优先做最有把握的题；
3. 一道题连续 15—20 分钟没有新进展，就暂时跳过；
4. 不因一道难题放弃整场比赛；
5. 比赛结束后再研究难题。

比赛成绩主要用于获取信息：

- 哪些知识忘了；
- 哪些题型不会识别；
- 哪些代码容易错；
- 哪些题不该花太久。

排名不是人格评价。

### AI 使用规则

允许 AI 帮助：

- 解释概念；
- 检查你的思路；
- 提供分层提示；
- 分析编译错误；
- 生成边界测试；
- 帮助对拍；
- 比较两种复杂度。

尽量避免：

- 还没思考就直接要完整解法；
- 复制 AI 代码后认为自己已经学会；
- 遇到错误就反复重新生成；
- 用 AI 替代自己写状态、推导和证明。

推荐提示方式：

```text
我目前认为题目的关键是……
我尝试了……
我卡在……
请只指出我的方向是否合理，并给一个小提示，不要直接给完整解法。
```

看过完整解法后，必须进行二次验证：

- 当天独立重写；
- 第二天不看题解重做；
- 一周后做变式题。

---

## 六、每周安排

一个普通学习周可以参考：

- 3 次算法训练，每次 1.5—2 小时；
- 1 次完整比赛或模拟；
- 1 次比赛复盘；
- 2 次课程和计算机基础学习；
- 2—3 次运动；
- 至少半天不安排高强度学习。

算法、课程、项目和生活不应该互相吞噬。

如果正在招新前冲刺，可以暂时增加算法比例，但给出明确结束时间。招新结束后重新调整。

---

## 七、招新之后的决策规则

### 如果进入 ACM 实验室

不要因为进入实验室就立刻无限投入。先观察一个学期：

- 是否有真实有效的训练体系；
- 是否有稳定队友和指导；
- 自己是否能持续进步；
- 是否开始稳定解决接近当前水平的题；
- 训练是否严重影响课程和身体；
- 是否仍然享受训练过程。

如果训练环境好、能力进步明显、兴趣稳定，可以提高竞赛投入。

### 如果没有进入

不要把结果解释为“我不适合算法”。

先判断：

- 是模板遗忘；
- 是比赛经验不足；
- 是基础知识缺口；
- 是代码实现不稳；
- 还是当前建模能力确实与要求存在较大差距。

之后可以继续以兴趣方式学算法，同时补充项目、基础课程和工程能力。

### 什么时候应该降低竞赛投入

出现以下情况时，应认真调整：

- 连续较长时间主要靠看题解；
- 训练时间不断增加，但独立能力几乎没有提高；
- 课程、睡眠和身体明显受损；
- 对竞赛本身没有兴趣，只是不甘心失败；
- 竞赛成绩没有形成实际机会，而项目和实习完全空白；
- 训练方式已经无法解释为什么能进步。

降低投入不是否定过去，而是根据新证据调整资源分配。

---

## 八、算法之外必须补的能力

无论是否进入 ACM，都应逐步建立：

- 一门主力语言；
- Git；
- Linux 基础；
- SQL 和数据库；
- HTTP 与接口；
- 调试和日志；
- 基本测试；
- 一个完整的小项目；
- 阅读文档和查资料的能力。

可以使用 AI 开发项目，但必须能：

- 解释主要数据流；
- 独立修改一个新功能；
- 定位至少一类真实错误；
- 写出基本测试；
- 部署并让别人使用。

竞赛训练的是抽象和解题，项目训练的是交付和维护。两者不能互相完全替代。

---

## 九、关于比较、权威和自我判断

我不需要拒绝所有比较。

可以比较：

- 哪种学习方式更有效；
- 哪种算法复杂度更低；
- 自己距离招新要求还有多大差距；
- 哪种投入更符合自己的目标；
- 训练方法调整后是否真的产生进步。

不需要比较：

- 自己是否比别人更有价值；
- 一次排名是否证明自己更聪明；
- 暂时落后是否说明人生失败；
- 今天没有超过昨天是否就没有意义。

面对老师、教练或其他权威的观点，可以采用四步：

1. 先准确复述对方的观点；
2. 分清这是事实、推理还是价值判断；
3. 写下自己原来的理由；
4. 决定接受、部分接受、保留或继续查证。

可以说：

> 我理解这个观点，但还没有判断它是否成立。

这不是软弱，而是暂缓交出判断权。

真正的独立思考不是永远反对权威，而是改变观点时知道自己为什么改变。

---

## 十、大学生活的底线

健康向上的生活不是每天都高强度，而是能够长期持续。

必须守住：

- 基本睡眠；
- 规律吃饭；
- 每周运动；
- 正常社交；
- 必要的独处和休息；
- 不因一次结果否定整体自我；
- 不把所有价值都放在成绩和排名上。

读书可以继续。阅读不必每次都产出观点，但可以偶尔写下：

```text
作者主张什么？
作者依靠什么理由？
我同意哪一部分？
我不同意哪一部分？
什么证据会让我改变看法？
```

“有自己的东西”不等于提出别人从未说过的观点，而是能够：

- 用自己的话表达；
- 说明依据；
- 看到边界；
- 承认未知；
- 在新证据出现时修正。

---

## 十一、每周复盘模板

每周末花 20 分钟填写：

```text
本周我独立完成了哪些题？

我看了题解但仍不会的题有哪些？

本周最常见的错误是什么？

我复习了哪些知识，能否闭卷实现？

我是否使用 AI 绕过了思考？

课程和基础是否被竞赛挤压？

睡眠、运动和情绪状态如何？

下周只选择哪三个重点？
```

下周重点最多三个。不要列十项计划，再用失败证明自己不够努力。

---

## 十二、长期判断

我不需要现在决定自己未来一定成为：

- ACM 竞赛选手；
- 普通软件工程师；
- AI 应用开发者；
- 算法研究者；
- 其他技术岗位从业者。

现在要做的是获得更多真实信息：

- 算法训练后，我是否持续进步？
- 我是否享受独立思考，而不只是享受看懂题解？
- 我能否完成真实项目？
- 我是否愿意面对调试、维护和重复工作？
- 哪些岗位和环境真正适合我？

当前最合理的策略是：

> 认真冲击这次 ACM 招新；恢复基础和独立解题能力；保留课程成绩；逐步补工程实践；根据连续几个月的真实反馈调整投入。

我不需要在算法和 AI 之间二选一。

更好的状态是：

- 用算法训练抽象思维；
- 用课程建立计算机基础；
- 用项目训练交付能力；
- 用 AI 提高学习和开发效率；
- 用真实反馈决定下一步，而不是被焦虑推动。

## 最后的一句话

一次比赛只说明一次比赛的结果。

它不能直接说明我有没有天赋、未来能不能就业、值不值得继续学习。

我真正需要观察的是：

> 在经过复盘和调整之后，我是否比之前更能独立理解、实现和验证问题。

如果答案是肯定的，就继续；如果答案是否定的，就改变方法，而不是简单地增加痛苦。
```

这份指南里最重要的判断是：**你现在可以认真冲 ACM 招新，但把“冲招新”定义成一个有期限、有反馈的阶段目标，而不是马上决定整个大学都 all in。**

你这场牛客赛只做出一道题，当前最值得做的事也不是继续堆更高级的算法，而是完成一次完整复盘，弄清楚剩下的题分别属于“知识忘了”“基础不会”“不会建模”还是“实现不稳”。这张分类表会比比赛总分更能决定你下一周该学什么。