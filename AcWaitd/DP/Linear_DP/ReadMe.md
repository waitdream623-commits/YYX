## 技巧
- 研究子数组或者子序列问题时：以某个位置为结尾，结合题意定义状态
- 阶段划分加上附加描述与属性构成状态
  - 阶段划分有向无环
  - 注意数量级，是否可以优化描述
  - 属性，初始化，与结果定义
---

## 前缀dp
- 是线性dp的一个子集，线性dp，依据阶段划分子状态，转移可能需要前面几个状态
- 状态转移仅由前一个状态转移
- 
### P12379 [蓝桥杯 2023 省 Python B] 松散子序列
- f[i],表示以0~i区间最大价值,如果不选，与i-1相同，选则找i-2；选i-2位置(尽可能多选)

### P12211 [蓝桥杯 2023 国 Python B] 翻转
- 附加属性，是否翻转，转移方程注意细节
### P1115 最大子段和

- **状态**：`f[i]` = 以 `a[i]` 结尾的最大子段和
- **转移**：`f[i] = max(a[i], f[i-1] + a[i])` —— 自己单干 / 接上之前
- **坑**：
  - 循环边界 `i<=n` 不是 `i<=1`
  - `ret` 初始化为 `-INF` 或 `a[1]`，不能是 `0`（全负情况输出 0）

### B3637 最长上升子序列（LIS）

#### O(n²) 做法
- **状态**：`f[i]` = 以 `a[i]` 结尾的 LIS 长度
- **转移**：`f[i] = max(f[j] + 1)` 其中 `j < i` 且 `a[i] > a[j]`
- **坑**：
  - 初始化所有 `f[i] = 1`，不能只初始化 `f[1]`
  - 答案是 `max(f[1..n])`，不是 `f[n]`——LIS 不一定以最后一个元素结尾

#### O(n log n) 贪心 + 二分
- 维护 `f[len]` = 长度为 len 的上升子序列的**末尾元素最小值**
- `f` 天然单调递增
- 遍历 `a[i]`：
  - `a[i] > f[len]` → `f[++len] = a[i]`（接上）
  - 否则 → `lower_bound(f+1, f+1+len, a[i])` 找到第一个 ≥ a[i] 的位置替换，使末尾尽可能小
- **非严格上升**：append 条件改为 `a[i] >= f[len]`，replace 用 `upper_bound`，upper让相等时尽可能更新更小，后续更容易接长


## P_3842_TJOI_2007_线段
- 状态定义f[N][2],f[i][0]：表示到达i层线段左端，最小步数。f[i][1]:表示到达第i层右端，最小步数
- 更新状态
~~~
左端，由上一层左端/或者右端到达+1(往下走一步)
计算注意是否最优，有坑，避免多走！！！
~~~
## P_1541_NOIP_2010_提高组_乌龟棋
- 4维状态定义

## P4059 [Code+#1] 找爸爸
- 跟最长公共子序列类似
- 状态定义要开第3维度，因为不清楚前面是否有空格，不满足无后效性，所以需要记录
- 两个都是空格显然不是一个优解，因为把这两个都去掉就必然会使得相似度增加。
- 故只有代表3种可能：没有空格/空格在A串/空格在B串。
- 遇到“后续决策的收益/成本取决于前驱节点是否处于同类连续动作中”的问题（例如：连续签到奖励递增、股票交易含固定冷冻期与手续费、带连击加成的游戏结算等），一律将动作的“生命周期阶段（未开始、进行中、刚结束）”提炼成 $0/1/2$ 状态机维度，将全局分段计费拆解为“开启成本”与“维系成本”进行局部状态转移。
### 通百道

最大子段和的框架改一下就是：

- **LIS O(n²)**：`dp[i] = max(dp[j] + 1)` 当 `a[j] < a[i]`，初始化 `dp[i]=1`
- **LIS O(n log n)**：`f[len]` 维护末尾最小值，`lower_bound` 替换
- **最大子矩阵**：把行压缩成一维，每列求和，然后跑最大子段和
- **最长连续递增子序列**：`dp[i] = dp[i-1] + 1` 当 `a[i] > a[i-1]`

**核心**：线性 DP 的状态往往定义成 **"以 i 结尾的 XXX"**，转移来源唯一——`i-1`。