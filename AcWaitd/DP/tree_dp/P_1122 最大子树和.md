# 题解
- 无根树，不能走回头路
- 向上返回信息，如何整合信息
- 类似最大字段和
- 状态定义f[x],表示以x为根，根节点必选，最大字段和
- f[i]<0 剪掉
- 但是，如果是状态表⽰为：⼦树内部的任意⼀个⼦树，状态转移⽅程是⽆法推导的。因为，并不知
道根节点的孩⼦在不在这个状态中。
• 因此，加上⼀个限制，研究这棵⼦树时，根节点必定要选。此时，就可以确定孩⼦的状态可不可
选。
• 那么，状态表⽰为： f[u] 表⽰：以 u 为根的⼦树，当根节点必选时，此时的最⼤美丽指数。
状态转移⽅程：
• 遍历所有孩⼦，只要最⼤美丽指数⼤于 0 ，就加上贡献。