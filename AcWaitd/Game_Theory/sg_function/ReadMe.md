# SG 函数（Sprague-Grundy 函数）

## 核心概念

将博弈分解为若干个**独立子博弈**，每个子博弈的 SG 值定义为：

$$
SG(x) = \operatorname{mex}\{\,SG(y) \mid x \to y \,\}
$$

其中 $\operatorname{mex}$ 表示集合中**未出现的最小非负整数**。

**Sprague-Grundy 定理**：
$$
\text{整个博弈的 SG 值} = SG_1 \oplus SG_2 \oplus \cdots \oplus SG_n
$$
先手必败 $\iff$ 总 SG 值为 $0$。
## 【SG 定理】
• 由 n 个有向图游戏组成的组合游戏，设起点分别为 s1 , s2 , ..., sn ，
• 当 SG(s1 )ˆSG(s2 )ˆ...ˆSG(sn) != 0 时，先⼿必胜；反之，先⼿必败。
也就是说，如果⼀个游戏是由若⼲个 ICG 游戏构成的，那么把每⼀个 ICG 游戏的 SG 值异或在⼀起，就
可以判断所有游戏下的胜负情况。

## 解题步骤
- 求mex值，将子问题加入unordered_map,去重，再从0枚举，找未出现过的
1. **划分子博弈**：将游戏分解为若干个互不影响的独立部分
2. **计算每个子博弈的 SG 值**：从终态反向递推或 DFS + 记忆化
3. **异或合并**：总 SG 为 $0$ 则先手必败

## 常见 SG 打表模式

```cpp
int sg[N];
bool vis[N];
void get_sg(int n) {
    for (int i = 1; i <= n; i++) {
        memset(vis, 0, sizeof vis);
        for (int j = 1; j <= m && i - j >= 0; j++)  // 所有合法转移
            vis[sg[i - j]] = 1;
        for (int j = 0;; j++)
            if (!vis[j]) { sg[i] = j; break; }
    }
}
```

## 题单

| 题号 | 题目 | 关键点 |
|------|------|--------|
| — | 移棋子游戏 | SG 模板，DAG 上计算 SG |
| — | Cutting Game | 二维分割，子博弈异或 |
| — | 取石子 |bash博弈，sg打表 |

## 利⽤ SG 函数解决 Bash 博奕
实际上，⼤部分的公平组合游戏都可以转换为有向图游戏。
- 当 Bash 游戏中的n与k确定之后，就可以把所有的状态图画出来。因为游戏⼀定能结束，所以必定是⼀个有向⽆环图。
那么，就可以⽤ SG 函数解决 Bash 博弈。

- 【利⽤ SG 函数，打表找规律】
对于很多 ICG 游戏，如果使⽤ SG 函数解决的话，时间复杂度很⾼。
对于上述 Bash 游戏，如果把所有的 SG 值打印出来，就能通过表格中数的规律，得到 Bash 游戏的结论。
## 【利⽤ SG 定理解决 Nim 游戏】
• Nim 游戏的每⼀堆⽯⼦，都可以看成是⼀个 ICG 游戏。因此 Nim 游戏本⾝就是由若⼲个 ICG 游戏组成的。
• 在每⼀个游戏中，SG 值都是⽯⼦个数。因此，Nim 游戏的结论和 SG 定理是对应的