# 思考
## 分析
- 道路双向，**重边**
- 编号从 **0** 开始
- 两个节点即村庄修好才可连通，~~需要标记~~(划线语句不能有标点符号？),只需要随着解锁城市依次更新Floyd，因为时间递增，不需要剔除前面更新结果
- 多次询问，Q(x,y,t):x->y,在t天最短距离，未通车输出 ***-1***
- 修复时间单调，询问时间单调，根据Floyd算法中第一层循环k，表示加入第k个节点每个节点间最短距离

## 关键
- 存储修复时间
- 邻接矩阵存储即，k=0时f[i][j]的存储
- 每次询问，第一次判断有几个节点，进行Floyd，后续节点在前一次上更新，标记k更新到哪儿，这次询问需要更新到。即k的起点和尾的更新
  
## 实操错误分析
- 开了一个edge存边，想着当该点解锁将边加入，假设点 A 和点 B 之间原本有一条距离为 10 的边，但通过早就修好的点 C 中转，距离只有 5。现在点 A 终于修好了，你执行了 f[A][B] = edge[A][B]（即赋为 10）。这就把之前好不容易算出来的最短路 5 给无情覆盖了！
- 找结束点,需要让a<n;
~~~cpp
 int a=0;
    while(t[a]<=end)a++;//找到此时已经修复了几个点
    //此时比endd大
    a--;
~~~
