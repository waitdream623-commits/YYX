# 题解

## 注意
- 存在负边权，只能用bf或者spfa
- 需要判断是否存在负环，bf循环次数，如果无负边权，最多进行n-1次
- spfa动态规划记录cnt[i]是否大于n-1
- 存边权需要存负数即取负，因为是减小距离
- 需要两次单源，从1->n,和从n->1;

## 错误回放
- bf枚举忘记枚举从1~n，少了n
- 不是无向图

## 收获
- vector可自动处理重边
  - 如果为邻接矩阵需要取min，防止后续覆盖
  - vector处理，会将二者都存入，在进行松弛操作，迟早会更新为最优解